# /dm/app/ — AWAITING Sub-AI output

Empty room reserved for the **app shell** — entry point, lifecycle,
session wiring. Authorship of these files belongs to whichever DM Sub-AI
DAI assigns; the most likely owners are **DM-Logic** (lifecycle / state)
and **DM-Auto** (headless entry point), with **DM-UI** owning the shell
layout if the app has a graphical surface.

The executor has placed nothing of substance here. No entry point, no
config, no bootstrap.

## Status

WIP — empty by design.
