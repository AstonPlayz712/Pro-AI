# /dm/assets/ — AWAITING reference device

Empty room reserved for the **reference device** — the canonical example
build that DM ships with. Authorship belongs to **DM-Logic** (component
spec), **DM-Realism** (plausibility check), **DM-Market** (target
segment), and **DM-UI** (preview asset).

The executor has placed nothing of substance here. No device JSON, no
preview image, no fixture data.

## Status

WIP — empty by design.
