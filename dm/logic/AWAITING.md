# /dm/logic/ — AWAITING Sub-AI output

This directory is the empty room reserved for **DM-Logic**, **DM-Realism**,
**DM-Market**, and **DM-Auto** outputs.

The executor has placed nothing of substance here. No models, no rules,
no scoring code. The first real files arrive when DAI dispatches the
Sub-AIs and their packets are transcribed verbatim.

## Reserved sub-namespaces (chosen by each Sub-AI on first dispatch)

- `/dm/logic/<dm-logic ns>/`     — DM-Logic
- `/dm/logic/<dm-realism ns>/`   — DM-Realism (constraints, references)
- `/dm/logic/<dm-market ns>/`    — DM-Market (segments, demand)
- `/dm/logic/<dm-auto ns>/`      — DM-Auto (headless harness pieces)

Naming, public surface, and module split are decisions reserved for the
respective Sub-AIs. The executor will not invent them.

## Status

WIP — empty by design.
