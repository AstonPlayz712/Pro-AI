# /dm/ui/ — AWAITING Sub-AI output

Empty room reserved for **DM-UI** and **DM-UX** outputs.

The executor has placed nothing of substance here. No screens, no widgets,
no theme tokens, no flow scripts.

## Reserved sub-namespaces

- `/dm/ui/<dm-ui ns>/`   — DM-UI (screens, widgets, theme)
- `/dm/ui/<dm-ux ns>/`   — DM-UX (flows, copy, interaction specs)

Framework, file format, and styling system are DM-UI / DM-UX decisions —
not the executor's. They will declare their choices in `/dm/README.md`
on first dispatch.

## Status

WIP — empty by design.
